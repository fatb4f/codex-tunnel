mcp.meowrz.uk {
	respond / "Codex remote bridge OK" 200

	reverse_proxy /mcp* 127.0.0.1:8000 {
		flush_interval -1
	}
	reverse_proxy /remote* 127.0.0.1:8000

	header /mcp* Cache-Control "no-store, no-transform"
	header /remote* Cache-Control "no-store, no-transform"
}
